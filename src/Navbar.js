import React from "react";
const Navbar = () => {
    return (
        <nav className="navbar">
            <a className="home" href="/">Home</a>
            <a className="add" href="/add">Add User</a>
        </nav>
    );
}
export default Navbar;
